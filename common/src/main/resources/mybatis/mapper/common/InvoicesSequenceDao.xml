<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edu.common.dao.InvoicesSequenceDao">
	
	<!-- 根据编号类型 查询编号序列号 -->
	<select id="querySequenceByInvoicesType" resultType="com.edu.common.model.InvoicesSequence" parameterType="java.lang.Long">
		SELECT * FROM tab_invoices_sequence t WHERE t.invoices_type = #{invoices_type}
	</select>
	
	<select id="selectSeq" parameterType="long" resultType="com.edu.common.model.InvoicesSequence">
		SELECT t.sequence, t.id
		FROM tab_invoices_sequence t
		WHERE t.invoices_type = #{invoiceType,jdbcType=NUMERIC};
	</select>

	<update id="updateSeq" parameterType="com.edu.common.model.InvoicesSequence">
		update tab_invoices_sequence set sequence = #{sequence,jdbcType=NUMERIC} + 1 where id = #{id,jdbcType=NUMERIC}
	</update>
	
	<!-- 如果未找到对应编号类型 则插入新编号类型 -->
	<insert id="insert" parameterType="com.edu.common.model.InvoicesSequence">
		INSERT INTO 
			tab_invoices_sequence (invoices_type, sequence)
		VALUES 
		(	
			#{invoices_type,jdbcType=NUMERIC},
			#{sequence,jdbcType=NUMERIC}
		)
	</insert>
	
	<!-- 根据编号类型 修改 编号序列内容 -->
	<update id="update" parameterType="com.edu.common.model.InvoicesSequence">
		UPDATE 
			tab_invoices_sequence
			<if test="invoices_type != null">
				SET sequence = #{sequence} WHERE invoices_type = #{invoices_type} 
			</if>
			<if test="invoices_type == null">
				SET sequence = 1
			</if>
	</update>
</mapper>