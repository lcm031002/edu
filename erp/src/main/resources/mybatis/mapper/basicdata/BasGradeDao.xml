<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edu.erp.dao.BasGradeDao">
	<select id="queryForPage" parameterType="map" resultType="com.edu.erp.model.BasGrade">
    	select t.id,
			t.encoding,
			t.grade_name as gradeName,
			t.last_id as lastId ,
			t.last_encoding as lastEncoding,
			t.last_grade_name as lastGradeName,
			t.sort_num as sortNum,
			t.status,
			description
		from t_bas_grade t
	</select>
</mapper>