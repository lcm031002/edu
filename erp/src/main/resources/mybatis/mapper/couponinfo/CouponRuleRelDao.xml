<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edu.erp.dao.CouponRuleRelDao">
	<insert id="addCouponRuleRel" parameterType="java.util.List">
		<foreach collection="list" item="item" index="index" open="begin" close="; end;" separator=";">
            INSERT INTO tab_student_gift_info 
            (
               encoding,
               student_id
            )
            VALUES
            (
               #{item.one_id,jdbcType=NUMERIC},
               #{item.much_id,jdbcType=VARCHAR}
            )
       </foreach>
	</insert>			
</mapper>